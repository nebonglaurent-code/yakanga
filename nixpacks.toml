[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.build]
cmds = [
  "pip install -r requirements.txt",
  "python manage.py collectstatic --noinput",
  "python manage.py migrate --run-syncdb"
]

[start]
cmd = "gunicorn core.wsgi --bind 0.0.0.0:$PORT --workers 2 --log-file - --log-level debug"